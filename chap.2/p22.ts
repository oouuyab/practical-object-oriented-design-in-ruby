{
  const chainRing = 52; // 톱니 개수
  const cog = 11;
  const ratio = chainRing / cog;
  console.log(ratio); // -> 4.72727272727273
}

{
  const chainRing = 30;
  const cog = 27;
  const ratio = chainRing / cog;
  console.log(ratio); // -> 1.11111111111111
}
